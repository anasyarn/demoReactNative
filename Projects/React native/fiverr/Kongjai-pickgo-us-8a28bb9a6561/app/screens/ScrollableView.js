import React, { Component } from 'react';
import { View, Text } from 'react-native';

class ScrollableView extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> ScrollableView </Text>
      </View>
    );
  }
}

export default ScrollableView;
